# Dallyrun 백엔드 베이스 URL
VITE_API_BASE_URL=http://localhost:8080

# 인메모리 mock 모드. 'true' 로 설정하면 모든 API 호출이 백엔드 대신
# src/api/mockApi.ts 의 핸들러로 라우팅된다. 백엔드 없이 전체 시퀀스
# (회원가입 → 로그인 → 마이페이지 → 로그아웃 → 회원탈퇴) 검증용.
# 기본값(미설정 또는 'false'): 실제 백엔드 호출.
VITE_USE_MOCK_API=false
