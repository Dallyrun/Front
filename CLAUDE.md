@AGENTS.md

## Claude Code

- Use `AGENTS.md` as the single source of truth for project rules, verification gates, and feedback loops.
- Store only personal or machine-local notes in Claude memory. Shared project rules must be committed to `AGENTS.md`, `.agents/`, `.github/`, or another reviewed repository file.
